# 个人词汇本 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 让用户在漫画阅读/分析的词汇卡上一键收藏词到个人生词本,并在 `/words` 页查看与删除收藏--为后续 JLPT 级别过滤、SRS、Anki 导出打数据基础。

**Architecture:** 纯客户端实现。`src/lib/word-bank.ts` 提供可单测的纯函数(去重 key / add / remove / toggle / isSaved / 构造);`src/lib/word-bank-store.ts` 用 zustand + `persist` 中间件把这些纯函数挂到 localStorage(`word-bank-storage`)。收藏入口加在共用组件 `MokuroAnalysisPanel` 的词卡上(三处分析视图全覆盖);`src/app/words/page.tsx` 是查看/删除页;`Header` 加链接与数量徽标。不动后端/API/OCR/分析缓存。

**Tech Stack:** Next.js App Router, React 19, TypeScript, Tailwind CSS, zustand 4(`persist`), Vitest(仅 `src/lib`), lucide-react 图标。

## Global Constraints

- 遵循项目约定:TypeScript、两空格缩进、单引号、无分号;`'use client'` 客户端组件;`@/` 别名导入 app 代码;PascalCase 组件文件、camelCase 函数。
- 去重 key = `JSON.stringify([word, reading])`(同形异音词如 橋/箸 各自独立,不误并)。
- 收藏时连带存「原句」= 传给 `MokuroAnalysisPanel` 的 `selectedText` prop(块级,无则 `null`)。
- `SavedWord.difficulty` 存为 `string`,不校验取值(与现有「LLM 生成、不校验」现状一致)。
- 不改 `src/lib/analysis-filters.ts`、后端、API、`ai-service.ts`、`mokuro-analysis-cache`;星标加在「过滤后」展示的词卡上。
- 日文文本节点保留 `lang="ja"` + `select-text` + `whitespace-pre-wrap break-words`(Yomitan 取词前提)。
- 双语文案:新增的 UI 文案补 `zh`/`en` 两套,沿用项目既有 `UI_TEXT satisfies Record<AnalysisLanguage, Record<string, string>>` 风格。
- 交付前跑 `npm test`、`npm run lint`、`npm run build`。

---

## File Structure

| 文件 | 职责 | 动作 |
|---|---|---|
| `src/lib/types.ts` | 加 `SavedWord` 接口(共享类型契约) | 修改 |
| `src/lib/word-bank.ts` | 纯函数:去重 key / add / remove / toggle / isSaved / toSavedWord。零依赖,可单测 | 新增 |
| `src/lib/word-bank.test.ts` | 覆盖纯函数行为 | 新增 |
| `src/lib/word-bank-store.ts` | zustand + persist,把纯函数挂到 localStorage | 新增 |
| `src/components/MokuroAnalysisPanel.tsx` | 词卡加星标按钮(收藏入口) | 修改 |
| `src/app/words/page.tsx` | 生词本查看/删除页 | 新增 |
| `src/components/Header.tsx` | 加「生词本」链接 + 数量徽标 | 修改 |

依赖顺序:Task 1(types) -> Task 2(纯函数+测试) -> Task 3(store) -> Task 4(收藏入口) -> Task 5(/words 页) -> Task 6(Header) -> Task 7(验证+收尾)。每个 Task 末尾独立可测、独立提交。

---

### Task 1: 加 `SavedWord` 共享类型

**Files:**
- Modify: `src/lib/types.ts`(在 `WordAnalysis` 接口之后新增)

**Interfaces:**
- Consumes: `WordAnalysis`(已存在于 `types.ts:1-7`,字段 `word/reading/meaning/partOfSpeech/difficulty`)
- Produces: `SavedWord` 接口,供 Task 2/3/4/5 使用

- [ ] **Step 1: 在 `src/lib/types.ts` 的 `WordAnalysis` 接口之后新增 `SavedWord`**

打开 `src/lib/types.ts`,定位到 `WordAnalysis` 接口(第 1-7 行):

```ts
export interface WordAnalysis {
  word: string
  reading: string
  meaning: string
  partOfSpeech: string
  difficulty: 'beginner' | 'intermediate' | 'advanced' | 'N5' | 'N4' | 'N3' | 'N2' | 'N1'
}
```

在其后(第 7 行 `}` 之后、第 9 行 `GrammarPattern` 之前)插入:

```ts
export interface SavedWord {
  word: string
  reading: string
  meaning: string
  partOfSpeech: string
  difficulty: string
  sourceSentence: string | null
  savedAt: string
}
```

- [ ] **Step 2: 类型检查**

Run: `npx tsc --noEmit`
Expected: 无新错误(项目可能存在预存的 `src/lib/*.test.ts` 报错,与本改动无关,保持不变;确认 `types.ts` 本身零报错)。

- [ ] **Step 3: Commit**

```bash
git add src/lib/types.ts
git commit -m "feat(word-bank): add SavedWord shared type

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: 纯函数 `word-bank.ts` + 单测(TDD)

**Files:**
- Create: `src/lib/word-bank.ts`
- Create: `src/lib/word-bank.test.ts`

**Interfaces:**
- Consumes: `WordAnalysis`、`SavedWord`(来自 `./types`)
- Produces: `savedWordKey`、`toSavedWord`、`addWord`、`removeWord`、`toggleWord`、`isSaved`(供 Task 3 store 调用)

- [ ] **Step 1: 写失败测试 `src/lib/word-bank.test.ts`**

```ts
import { describe, expect, it } from 'vitest'
import {
  addWord,
  isSaved,
  removeWord,
  savedWordKey,
  toSavedWord,
  toggleWord
} from './word-bank'
import type { SavedWord, WordAnalysis } from './types'

const baseWord: WordAnalysis = {
  word: '橋',
  reading: 'はし',
  meaning: '桥',
  partOfSpeech: '名词',
  difficulty: 'N3'
}

const makeSaved = (overrides: Partial<SavedWord> = {}): SavedWord => ({
  word: baseWord.word,
  reading: baseWord.reading,
  meaning: baseWord.meaning,
  partOfSpeech: baseWord.partOfSpeech,
  difficulty: baseWord.difficulty,
  sourceSentence: 'あの橋を渡る',
  savedAt: '2026-07-10T00:00:00.000Z',
  ...overrides
})

describe('savedWordKey', () => {
  it('same word + reading -> same key', () => {
    expect(savedWordKey('橋', 'はし')).toBe(savedWordKey('橋', 'はし'))
  })

  it('same word, different reading -> different key (handles homographs)', () => {
    expect(savedWordKey('橋', 'はし')).not.toBe(savedWordKey('橋', 'はしbogus'))
  })

  it('does not collide on concatenation edge cases', () => {
    // 'ab'+'c' vs 'a'+'bc' must NOT share a key
    expect(savedWordKey('ab', 'c')).not.toBe(savedWordKey('a', 'bc'))
  })
})

describe('toSavedWord', () => {
  it('carries WordAnalysis fields and sourceSentence, injects savedAt', () => {
    const saved = toSavedWord(baseWord, 'あの橋を渡る', '2026-07-10T00:00:00.000Z')
    expect(saved).toEqual(makeSaved())
  })

  it('allows null sourceSentence', () => {
    const saved = toSavedWord(baseWord, null, '2026-07-10T00:00:00.000Z')
    expect(saved.sourceSentence).toBeNull()
  })
})

describe('addWord', () => {
  it('prepends a new word', () => {
    const result = addWord([], makeSaved())
    expect(result).toHaveLength(1)
    expect(result[0].word).toBe('橋')
  })

  it('does not duplicate when key already exists', () => {
    const existing = makeSaved()
    const result = addWord([existing], makeSaved({ savedAt: '2026-07-11T00:00:00.000Z' }))
    expect(result).toHaveLength(1)
    expect(result[0].savedAt).toBe('2026-07-10T00:00:00.000Z') // original kept
  })

  it('keeps newer entries toward the front (prepend)', () => {
    const first = makeSaved({ word: '先', reading: 'せん', savedAt: '2026-07-09T00:00:00.000Z' })
    const second = makeSaved({ word: '後', reading: 'ご', savedAt: '2026-07-10T00:00:00.000Z' })
    expect(addWord([first], second)[0].word).toBe('後')
  })
})

describe('removeWord', () => {
  it('removes by word + reading', () => {
    const list = [makeSaved()]
    expect(removeWord(list, '橋', 'はし')).toEqual([])
  })

  it('is a no-op when key absent', () => {
    const list = [makeSaved()]
    expect(removeWord(list, '箸', 'はし')).toEqual(list)
  })
})

describe('toggleWord', () => {
  it('adds when absent', () => {
    const result = toggleWord([], makeSaved())
    expect(result).toHaveLength(1)
  })

  it('removes when present', () => {
    const list = [makeSaved()]
    expect(toggleWord(list, makeSaved())).toEqual([])
  })
})

describe('isSaved', () => {
  it('returns true when present', () => {
    expect(isSaved([makeSaved()], '橋', 'はし')).toBe(true)
  })

  it('returns false when absent', () => {
    expect(isSaved([makeSaved()], '箸', 'はし')).toBe(false)
  })
})
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npx vitest run src/lib/word-bank.test.ts`
Expected: FAIL,报错 `Failed to resolve import "./word-bank"`(模块不存在)。

- [ ] **Step 3: 写最小实现 `src/lib/word-bank.ts`**

```ts
import type { SavedWord, WordAnalysis } from './types'

export const savedWordKey = (word: string, reading: string): string =>
  JSON.stringify([word, reading])

export const toSavedWord = (
  word: WordAnalysis,
  sourceSentence: string | null,
  savedAt: string
): SavedWord => ({
  word: word.word,
  reading: word.reading,
  meaning: word.meaning,
  partOfSpeech: word.partOfSpeech,
  difficulty: word.difficulty,
  sourceSentence,
  savedAt
})

export const addWord = (words: SavedWord[], entry: SavedWord): SavedWord[] => {
  const key = savedWordKey(entry.word, entry.reading)
  if (words.some(w => savedWordKey(w.word, w.reading) === key)) {
    return words
  }
  return [entry, ...words]
}

export const removeWord = (
  words: SavedWord[],
  word: string,
  reading: string
): SavedWord[] => {
  const key = savedWordKey(word, reading)
  return words.filter(w => savedWordKey(w.word, w.reading) !== key)
}

export const toggleWord = (words: SavedWord[], entry: SavedWord): SavedWord[] => {
  const key = savedWordKey(entry.word, entry.reading)
  if (words.some(w => savedWordKey(w.word, w.reading) === key)) {
    return removeWord(words, entry.word, entry.reading)
  }
  return addWord(words, entry)
}

export const isSaved = (
  words: SavedWord[],
  word: string,
  reading: string
): boolean => {
  const key = savedWordKey(word, reading)
  return words.some(w => savedWordKey(w.word, w.reading) === key)
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npx vitest run src/lib/word-bank.test.ts`
Expected: PASS,全部用例绿。

- [ ] **Step 5: 类型检查 + lint**

Run: `npx tsc --noEmit`
Expected: 无新错误。

Run: `npm run lint`
Expected: 0 错误(预存的 `<img>` 警告不变)。

- [ ] **Step 6: Commit**

```bash
git add src/lib/word-bank.ts src/lib/word-bank.test.ts
git commit -m "feat(word-bank): add pure functions with tests

savedWordKey uses JSON.stringify([word, reading]) to avoid homograph
and concatenation collisions. addWord dedups and prepends; toggleWord
is bidirectional; toSavedWord injects savedAt to keep functions pure.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: zustand store `word-bank-store.ts`

**Files:**
- Create: `src/lib/word-bank-store.ts`

**Interfaces:**
- Consumes: `WordAnalysis`(from `./types`)、`addWord`/`removeWord`/`toggleWord`/`isSaved`/`toSavedWord`(from `./word-bank`)
- Produces: `useWordBankStore` hook,方法签名见下;供 Task 4/5/6 调用

- [ ] **Step 1: 写 `src/lib/word-bank-store.ts`**

```ts
import { create } from 'zustand'
import { persist } from 'zustand/middleware'
import type { SavedWord, WordAnalysis } from './types'
import { addWord, isSaved, removeWord, toSavedWord, toggleWord } from './word-bank'

interface WordBankState {
  words: SavedWord[]
  addWord: (word: WordAnalysis, sourceSentence: string | null) => void
  removeWord: (word: string, reading: string) => void
  toggleWord: (word: WordAnalysis, sourceSentence: string | null) => void
  isSaved: (word: string, reading: string) => boolean
  clearAll: () => void
}

export const useWordBankStore = create<WordBankState>()(persist(
  (set, get) => ({
    words: [],
    addWord: (word, sourceSentence) => set(state => ({
      words: addWord(state.words, toSavedWord(word, sourceSentence, new Date().toISOString()))
    })),
    removeWord: (word, reading) => set(state => ({
      words: removeWord(state.words, word, reading)
    })),
    toggleWord: (word, sourceSentence) => set(state => ({
      words: toggleWord(state.words, toSavedWord(word, sourceSentence, new Date().toISOString()))
    })),
    isSaved: (word, reading) => isSaved(get().words, word, reading),
    clearAll: () => set({ words: [] })
  }),
  {
    name: 'word-bank-storage',
    version: 1
  }
))
```

- [ ] **Step 2: 类型检查 + lint**

Run: `npx tsc --noEmit`
Expected: 无新错误。

Run: `npm run lint`
Expected: 0 错误。

- [ ] **Step 3: Commit**

```bash
git add src/lib/word-bank-store.ts
git commit -m "feat(word-bank): add zustand store with localStorage persistence

Thin wrapper over word-bank pure functions; savedAt timestamp injected
at call time so pure layer stays testable. Persisted to
word-bank-storage v1.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: `MokuroAnalysisPanel` 词卡加星标收藏入口

**Files:**
- Modify: `src/components/MokuroAnalysisPanel.tsx`(imports + 词卡 `<li>` 渲染)

**Interfaces:**
- Consumes: `useWordBankStore`(from `@/lib/word-bank-store`)、`WordAnalysis`(from `@/lib/types`)、`Star` 图标(lucide-react)、props `selectedText`(已存在)
- Produces: 词卡上的星标按钮,点击 toggle 收藏;已收藏态实心琥珀星

- [ ] **Step 1: 加 imports 与收藏子组件**

打开 `src/components/MokuroAnalysisPanel.tsx`。当前 import 行(第 1-6 行):

```tsx
'use client'

import { motion } from 'framer-motion'
import { BookOpen, Loader2, Quote, Sparkles } from 'lucide-react'
import { filterLearningGrammar, filterLearningVocabulary } from '@/lib/analysis-filters'
import type { AnalysisLanguage, AnalysisResult } from '@/lib/types'
```

改为:

```tsx
'use client'

import { motion } from 'framer-motion'
import { BookOpen, Loader2, Quote, Sparkles, Star } from 'lucide-react'
import { filterLearningGrammar, filterLearningVocabulary } from '@/lib/analysis-filters'
import { useWordBankStore } from '@/lib/word-bank-store'
import type { AnalysisLanguage, AnalysisResult, WordAnalysis } from '@/lib/types'
```

- [ ] **Step 2: 在 `MokuroAnalysisPanel` 组件内、`return` 之前接入 store,并加一个词卡收藏按钮子组件**

定位到组件签名(约第 68-75 行):

```tsx
export default function MokuroAnalysisPanel({
  analysisResult,
  isAnalyzing,
  selectedText,
  language,
  hideSelectedText = false
}: MokuroAnalysisPanelProps) {
  const t = UI_TEXT[language]
```

在其后(`const t = UI_TEXT[language]` 之后)插入 store 钩子:

```tsx
  const toggleWord = useWordBankStore(state => state.toggleWord)
```

然后在 `MokuroAnalysisPanel` 组件**之外**(文件底部、`export default function` 闭合之后)新增一个内部子组件 `WordSaveButton`,用于单个词卡的收藏按钮:

```tsx
interface WordSaveButtonProps {
  word: WordAnalysis
  sourceSentence: string | null
}

function WordSaveButton({ word, sourceSentence }: WordSaveButtonProps) {
  const isSaved = useWordBankStore(state => state.isSaved)
  const toggleWord = useWordBankStore(state => state.toggleWord)
  const saved = isSaved(word.word, word.reading)

  return (
    <button
      type="button"
      onClick={() => toggleWord(word, sourceSentence)}
      aria-label={saved ? 'Remove from word bank' : 'Save to word bank'}
      aria-pressed={saved}
      className={`shrink-0 rounded-full p-1 transition-colors hover:bg-white/10 ${
        saved ? 'text-amber-300' : 'text-gray-500'
      }`}
    >
      <Star size={16} className={saved ? 'fill-current' : ''} />
    </button>
  )
}
```

> 用独立子组件订阅 `isSaved`/`toggleWord`,使每个词卡各自订阅 store、避免整张词表因单个收藏动作重渲染(缩小重渲染范围)。

- [ ] **Step 3: 在词卡渲染中挂上 `WordSaveButton`**

定位词卡 `<li>`(约第 172-191 行):

```tsx
            {learningVocabulary.map((word, index) => (
              <li
                key={`${word.word}-${index}`}
                className="rounded-lg border border-white/10 bg-gray-950/40 p-3"
              >
                <div className="flex items-start justify-between gap-3">
                  <div>
                    <p className="font-japanese font-semibold text-white">
                      {word.word}
                    </p>
                    <p className="text-xs text-gray-400">{word.reading}</p>
                  </div>
                  <span className={`rounded-full border px-2 py-0.5 text-xs font-medium ${DIFFICULTY_BADGE_CLASSES[normalizeDifficulty(word.difficulty)] ?? FALLBACK_DIFFICULTY_CLASS}`}>
                    {word.difficulty}
                  </span>
                </div>
                <p className="mt-1 text-sm text-gray-100">{word.meaning}</p>
                <p className="mt-0.5 text-xs text-gray-500">{word.partOfSpeech}</p>
              </li>
            ))}
```

把词与难度 badge 之间的那一行 `<div className="flex items-start justify-between gap-3">...</div>` 改为「词 + 右侧(难度 badge 与星标按钮一组)」。替换为:

```tsx
            {learningVocabulary.map((word, index) => (
              <li
                key={`${word.word}-${index}`}
                className="rounded-lg border border-white/10 bg-gray-950/40 p-3"
              >
                <div className="flex items-start justify-between gap-3">
                  <div>
                    <p className="font-japanese font-semibold text-white">
                      {word.word}
                    </p>
                    <p className="text-xs text-gray-400">{word.reading}</p>
                  </div>
                  <div className="flex items-center gap-1">
                    <span className={`rounded-full border px-2 py-0.5 text-xs font-medium ${DIFFICULTY_BADGE_CLASSES[normalizeDifficulty(word.difficulty)] ?? FALLBACK_DIFFICULTY_CLASS}`}>
                      {word.difficulty}
                    </span>
                    <WordSaveButton word={word} sourceSentence={selectedText} />
                  </div>
                </div>
                <p className="mt-1 text-sm text-gray-100">{word.meaning}</p>
                <p className="mt-0.5 text-xs text-gray-500">{word.partOfSpeech}</p>
              </li>
            ))}
```

要点:① 难度 badge 与星标按钮包进同一个 `<div className="flex items-center gap-1">`;② `sourceSentence={selectedText}` 传当前面板的 `selectedText`(块级原句,Task 1 时已确认是现有 prop);③ 词卡 `key` 沿用 `${word.word}-${index}` 不变。

- [ ] **Step 4: 类型检查 + lint**

Run: `npx tsc --noEmit`
Expected: 无新错误。

Run: `npm run lint`
Expected: 0 错误(确认 `Star` 已被使用、`toggleWord` 在主组件的 import 钩子被使用--若 lint 报主组件 `toggleWord` 未用,删除主组件里那行 `const toggleWord = useWordBankStore(...)`,只保留 `WordSaveButton` 内的订阅。`WordAnalysis` import 用于 `WordSaveButtonProps`)。

- [ ] **Step 5: 手动验证**

Run: `npm run dev`,在 Mokuro Reader 选一个块分析(或图片分析模式),在词汇卡:
① 未收藏词 -> 灰色空心星;点击 -> 变琥珀实心星;
② 再点 -> 变回空心;
③ 刷新页面后,已收藏的词仍是实心(localStorage 持久化);
④ 同形异音词(若该块有)各自独立收藏。
若无可用数据,至少确认页面无运行时报错、星标按钮可点。

- [ ] **Step 6: Commit**

```bash
git add src/components/MokuroAnalysisPanel.tsx
git commit -m "feat(word-bank): star-to-save button on vocabulary cards

Add a per-card WordSaveButton in the shared MokuroAnalysisPanel so the
save action reaches Mokuro Reader, ReadingModeViewer, and
ImagePageAnalysisViewer at once. sourceSentence captures the panel's
selectedText (block-level sentence).

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: `/words` 生词本查看/删除页

**Files:**
- Create: `src/app/words/page.tsx`

**Interfaces:**
- Consumes: `useWordBankStore`(from `@/lib/word-bank-store`)、`SavedWord`(from `@/lib/types`)、`BookOpen`/`Trash2`/`Star` 图标、Next.js `Link`
- Produces: `/words` 路由,列出收藏词、支持单条删除与清空

- [ ] **Step 1: 写 `src/app/words/page.tsx`**

```tsx
'use client'

import { BookOpen, Star, Trash2 } from 'lucide-react'
import Link from 'next/link'
import { useWordBankStore } from '@/lib/word-bank-store'
import type { AnalysisLanguage, SavedWord } from '@/lib/types'

const UI_TEXT = {
  zh: {
    title: '生词本',
    count: '个词',
    empty: '还没有收藏任何词',
    emptyBody: '阅读时点词汇卡上的星标,把不认识的词收进来吧。',
    backHome: '返回首页',
    clearAll: '清空全部',
    clearConfirm: '确定要清空全部收藏的词吗?此操作不可撤销。',
    noSentence: '(无原句)',
    justNow: '刚刚',
    minutesAgo: '分钟前',
    hoursAgo: '小时前',
    daysAgo: '天前'
  },
  en: {
    title: 'Word Bank',
    count: 'words',
    empty: 'No saved words yet',
    emptyBody: 'Tap the star on a vocabulary card while reading to save words you don\'t know.',
    backHome: 'Back to home',
    clearAll: 'Clear all',
    clearConfirm: 'Clear all saved words? This cannot be undone.',
    noSentence: '(no source sentence)',
    justNow: 'just now',
    minutesAgo: 'min ago',
    hoursAgo: 'h ago',
    daysAgo: 'd ago'
  }
} satisfies Record<AnalysisLanguage, Record<string, string>>

// 本页默认中文文案(与 Image Analyzer 默认中文一致);language 固定 zh,不引入语言切换。
const language: AnalysisLanguage = 'zh'
const t = UI_TEXT[language]

const DIFFICULTY_BADGE_CLASSES: Record<string, string> = {
  beginner: 'bg-green-500/15 text-green-300 border-green-500/25',
  n5: 'bg-green-500/15 text-green-300 border-green-500/25',
  n4: 'bg-green-500/15 text-green-300 border-green-500/25',
  intermediate: 'bg-yellow-500/15 text-yellow-300 border-yellow-500/25',
  n3: 'bg-yellow-500/15 text-yellow-300 border-yellow-500/25',
  n2: 'bg-yellow-500/15 text-yellow-300 border-yellow-500/25',
  advanced: 'bg-red-500/15 text-red-300 border-red-500/25',
  n1: 'bg-red-500/15 text-red-300 border-red-500/25'
}
const FALLBACK_DIFFICULTY_CLASS = 'bg-gray-500/15 text-gray-300 border-gray-500/25'
const normalizeDifficulty = (difficulty: string): string => difficulty.toLowerCase()

const relativeTime = (iso: string): string => {
  const then = new Date(iso).getTime()
  if (Number.isNaN(then)) return ''
  const now = Date.now()
  const diffMs = now - then
  const min = Math.floor(diffMs / 60000)
  if (min < 1) return t.justNow
  if (min < 60) return `${min} ${t.minutesAgo}`
  const hr = Math.floor(min / 60)
  if (hr < 24) return `${hr} ${t.hoursAgo}`
  const day = Math.floor(hr / 24)
  return `${day} ${t.daysAgo}`
}

function WordCard({ word }: { word: SavedWord }) {
  const removeWord = useWordBankStore(state => state.removeWord)
  return (
    <li className="rounded-lg border border-white/10 bg-gray-950/40 p-3">
      <div className="flex items-start justify-between gap-3">
        <div>
          <p className="font-japanese font-semibold text-white">{word.word}</p>
          <p className="text-xs text-gray-400">{word.reading}</p>
        </div>
        <div className="flex items-center gap-1">
          <span className={`rounded-full border px-2 py-0.5 text-xs font-medium ${DIFFICULTY_BADGE_CLASSES[normalizeDifficulty(word.difficulty)] ?? FALLBACK_DIFFICULTY_CLASS}`}>
            {word.difficulty}
          </span>
          <button
            type="button"
            onClick={() => removeWord(word.word, word.reading)}
            aria-label="Remove from word bank"
            className="shrink-0 rounded-full p-1 text-gray-500 transition-colors hover:bg-white/10 hover:text-red-300"
          >
            <Trash2 size={16} />
          </button>
        </div>
      </div>
      <p className="mt-1 text-sm text-gray-100">{word.meaning}</p>
      <p className="mt-0.5 text-xs text-gray-500">{word.partOfSpeech}</p>
      {word.sourceSentence ? (
        <p
          lang="ja"
          className="mt-2 border-t border-white/10 pt-2 font-japanese text-xs leading-relaxed text-gray-300 whitespace-pre-wrap break-words select-text"
        >
          {word.sourceSentence}
        </p>
      ) : (
        <p className="mt-2 border-t border-white/10 pt-2 text-xs text-gray-600">{t.noSentence}</p>
      )}
      <p className="mt-1 text-xs text-gray-600">{relativeTime(word.savedAt)}</p>
    </li>
  )
}

export default function WordBankPage() {
  const words = useWordBankStore(state => state.words)
  const clearAll = useWordBankStore(state => state.clearAll)

  const handleClear = () => {
    if (window.confirm(t.clearConfirm)) {
      clearAll()
    }
  }

  return (
    <div className="container mx-auto px-4 py-8">
      <div className="mb-6 flex items-center gap-2">
        <BookOpen size={20} className="text-amber-300" />
        <h1 className="text-xl font-bold text-white">{t.title}</h1>
        <span className="text-sm text-gray-400">{words.length} {t.count}</span>
        {words.length > 0 && (
          <button
            type="button"
            onClick={handleClear}
            className="ml-auto rounded-lg border border-white/10 px-3 py-1 text-sm text-gray-300 transition-colors hover:bg-white/10"
          >
            {t.clearAll}
          </button>
        )}
      </div>

      {words.length > 0 ? (
        <ul className="space-y-2">
          {words.map(word => (
            <WordCard key={`${word.word}-${word.reading}`} word={word} />
          ))}
        </ul>
      ) : (
        <div className="rounded-2xl border border-white/10 bg-white/5 p-8 text-center">
          <Star className="mx-auto mb-3 h-8 w-8 text-gray-500" />
          <h2 className="font-semibold text-white">{t.empty}</h2>
          <p className="mt-1 text-sm text-gray-400">{t.emptyBody}</p>
          <Link
            href="/"
            className="mt-4 inline-block rounded-lg border border-white/10 px-4 py-2 text-sm text-gray-200 transition-colors hover:bg-white/10"
          >
            {t.backHome}
          </Link>
        </div>
      )}
    </div>
  )
}
```

要点:① `words` 已由 store 按 `savedAt` 倒序(`addWord` 前置插入),直接渲染即最新在前;② `WordCard` 独立订阅 `removeWord`,缩小删除时的重渲染范围;③ `key` 用 `${word.word}-${word.reading}`(去重 key 保证唯一);④ 原句 `<p>` 保留 `lang="ja"` + `select-text` + `whitespace-pre-wrap break-words`;⑤ `clearAll` 带 `window.confirm`;⑥ `relativeTime` 在渲染期取 `Date.now()`,仅用于显示(非持久状态)。

- [ ] **Step 2: 类型检查 + lint**

Run: `npx tsc --noEmit`
Expected: 无新错误。

Run: `npm run lint`
Expected: 0 错误(预存的 `<img>` 警告不变)。

- [ ] **Step 3: 手动验证**

Run: `npm run dev`,访问 `http://localhost:3000/words`:
① 先在阅读页收藏几个词,回到 `/words` -> 列表显示,含原句与相对时间,最新在前;
② 点单条垃圾桶 -> 该条消失,数量减少;
③ 「清空全部」-> 弹确认 -> 确认后清空,显示空状态 + 返回首页链接;
④ 空状态时无「清空全部」按钮;
⑤ 点「返回首页」-> 回到 `/`;
⑥ 刷新 `/words`,列表与 localStorage 一致。

- [ ] **Step 4: Commit**

```bash
git add src/app/words/page.tsx
git commit -m "feat(word-bank): add /words page to view and delete saved words

Lists saved words newest-first with source sentence and relative time.
Per-card delete and a confirm-guarded clear-all. Source sentence keeps
lang=ja + select-text for Yomitan lookup.

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: `Header` 加「生词本」链接与数量徽标

**Files:**
- Modify: `src/components/Header.tsx`

**Interfaces:**
- Consumes: `useWordBankStore`(from `@/lib/word-bank-store`)、Next.js `Link`、`BookOpen` 图标(已 import)

- [ ] **Step 1: 加 imports 与 store 订阅**

打开 `src/components/Header.tsx`。当前 import(第 1-4 行):

```tsx
'use client'

import { BookOpen, Github, Heart } from 'lucide-react'
import { motion } from 'framer-motion'
```

改为:

```tsx
'use client'

import { BookOpen, Github, Heart } from 'lucide-react'
import { motion } from 'framer-motion'
import Link from 'next/link'
import { useWordBankStore } from '@/lib/word-bank-store'
```

在组件内、`return` 之前接入数量:

```tsx
export default function Header() {
  const wordCount = useWordBankStore(state => state.words.length)
  return (
```

- [ ] **Step 2: 在右侧组加入「生词本」链接**

定位右侧组(约第 26-43 行):

```tsx
          <div className="flex items-center space-x-4">
            <motion.a
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              href="https://github.com"
              className="p-2 hover:bg-white/10 rounded-lg transition-colors"
              target="_blank"
              rel="noopener noreferrer"
            >
              <Github className="w-5 h-5 text-gray-400" />
            </motion.a>
            
            <div className="flex items-center space-x-1 text-sm text-gray-400">
              <span>Made with</span>
              <Heart className="w-4 h-4 text-red-400 fill-current" />
              <span>for learners</span>
            </div>
          </div>
```

在 GitHub 图标 `<motion.a>...</motion.a>` **之前**插入「生词本」链接(带数量徽标):

```tsx
          <div className="flex items-center space-x-4">
            <Link
              href="/words"
              className="flex items-center gap-1.5 rounded-lg px-3 py-1.5 text-sm text-gray-200 transition-colors hover:bg-white/10"
            >
              <BookOpen className="w-4 h-4" />
              <span>生词本</span>
              {wordCount > 0 && (
                <span className="rounded-full bg-amber-500/20 px-1.5 py-0.5 text-xs font-medium text-amber-300">
                  {wordCount}
                </span>
              )}
            </Link>

            <motion.a
              whileHover={{ scale: 1.05 }}
              whileTap={{ scale: 0.95 }}
              href="https://github.com"
              className="p-2 hover:bg-white/10 rounded-lg transition-colors"
              target="_blank"
              rel="noopener noreferrer"
            >
              <Github className="w-5 h-5 text-gray-400" />
            </motion.a>
            
            <div className="flex items-center space-x-1 text-sm text-gray-400">
              <span>Made with</span>
              <Heart className="w-4 h-4 text-red-400 fill-current" />
              <span>for learners</span>
            </div>
          </div>
```

要点:① `BookOpen` 已在 import 中,直接复用;② 数量徽标仅 `wordCount > 0` 时显示;③ 文案「生词本」固定中文(Header 当前无语言切换,与现状一致)。

- [ ] **Step 3: 类型检查 + lint**

Run: `npx tsc --noEmit`
Expected: 无新错误。

Run: `npm run lint`
Expected: 0 错误。

- [ ] **Step 4: 手动验证**

Run: `npm run dev`,任意页面:
① Header 出现「生词本」链接(含 BookOpen 图标);
② 无收藏时无数字徽标;收藏 1 个后出现 `1`;再收藏出现 `2`;删除后数字同步减少;
③ 点「生词本」-> 跳到 `/words`。

- [ ] **Step 5: Commit**

```bash
git add src/components/Header.tsx
git commit -m "feat(word-bank): add word bank link + count badge to header

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 7: 全量验证与收尾

**Files:**
- 无代码改动(仅验证);若 lint/build 暴露问题回到对应 Task 修。

- [ ] **Step 1: 全量测试**

Run: `npm test`
Expected: 全绿(含新增 `word-bank.test.ts`)。

- [ ] **Step 2: lint**

Run: `npm run lint`
Expected: 0 错误(预存的 `<img>` 警告不变)。

- [ ] **Step 3: 生产构建**

Run: `npm run build`
Expected: 构建成功,`/words` 路由出现在路由清单中,无类型错误。

- [ ] **Step 4: 端到端手动验证(完整流程)**

Run: `npm run dev`,按 spec「手动验证」矩阵逐项确认:
1. Mokuro Reader:分析块,词卡点星标 -> 实心;再点 -> 空心。
2. 图片分析(阅读模式 + 整页 fallback):星标可用。
3. Header 徽标数量随收藏实时变化。
4. `/words` 列出已收藏词,含原句与相对时间,按时间倒序。
5. `/words` 删除单条 -> 列表与徽标同步减少。
6. `/words`「清空全部」带确认,确认后清空。
7. 刷新后收藏仍在。
8. 重复收藏同一词不产生重复条目。
9. 同形异音词(橋/箸)各自独立收藏,不误并。
10. `lang="ja"` + `select-text` 在 `/words` 原句与面板词上保留,Yomitan 可取词。

- [ ] **Step 5: 收尾确认**

确认所有改动文件已提交(`git status` 干净),无遗留调试代码。

---

## Self-Review 记录

- **Spec 覆盖:** ① 数据模型 `SavedWord`(Task 1)✓ ② 纯函数 + 去重 key `JSON.stringify([word, reading])`(Task 2)✓ ③ zustand+persist store(Task 3)✓ ④ 收藏入口 `MokuroAnalysisPanel` 星标(Task 4)✓ ⑤ `/words` 查看页(Task 5)✓ ⑥ Header 链接+徽标(Task 6)✓ ⑦ 测试 `word-bank.test.ts`(Task 2)✓ ⑧ `npm test`/`lint`/`build`(Task 7)✓ ⑨ 不动后端/API/缓存/`analysis-filters`(全局约束)✓ ⑩ 原句粒度 A(块级 `selectedText`)(Task 4 Step 3 `sourceSentence={selectedText}`)✓。
- **Placeholder 扫描:** 无 TBD/TODO;所有代码步骤含完整可执行代码;手动验证步骤含具体预期。Task 4 Step 2 的「主组件 `toggleWord`」一处注明了 lint 兜底处理(若未用则删),非占位,是实现时按 lint 结果的明确处置。
- **类型一致性:** `SavedWord` 字段(word/reading/meaning/partOfSpeech/difficulty/sourceSentence/savedAt)在 Task 1 定义,Task 2 `toSavedWord` 产出、Task 3 store 传递、Task 5 `WordCard` 消费,全部一致。纯函数签名 `addWord(words, entry)`/`removeWord(words, word, reading)`/`toggleWord(words, entry)`/`isSaved(words, word, reading)`/`toSavedWord(word, sourceSentence, savedAt)`/`savedWordKey(word, reading)` 在 Task 2 定义、Task 3 store 调用,签名一致。store 动作 `addWord(word, sourceSentence)`/`removeWord(word, reading)`/`toggleWord(word, sourceSentence)`/`isSaved(word, reading)`/`clearAll()` 在 Task 3 定义,Task 4(`toggleWord`/`isSaved`)、Task 5(`words`/`removeWord`/`clearAll`)、Task 6(`words.length`)消费,一致。
- **潜在副作用:** ① `MokuroAnalysisPanel` 是共用组件,改它影响 Mokuro Reader/ReadingModeViewer/ImagePageAnalysisViewer 三处--均属预期(收藏入口要全覆盖)。② 主组件若保留 `const toggleWord = useWordBankStore(...)` 但不用会触发 lint 未使用变量;Task 4 Step 4 已注明按 lint 结果删除该行(收藏逻辑全在 `WordSaveButton` 子组件内,主组件不需要它)。③ `relativeTime` 用 `Date.now()` 渲染显示时间,非持久状态,不影响测试。④ localStorage key `word-bank-storage` 与既有 `ai-provider-storage` 不冲突。
